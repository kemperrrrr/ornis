[package]
name = "ornis-rhai"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
ornis-core = { path = "../core" }
# `sync`: Arc/RwLock internals so Engine/AST are Send+Sync as the
# ScriptEngine trait requires.
rhai = { version = "1.26", features = ["sync"] }
serde_json = "1"

[lints]
workspace = true
