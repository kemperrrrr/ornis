[package]
name = "ornis-render"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
wgpu = "30"
bytemuck = { version = "1", features = ["derive"] }
glam = "0.33"
ornis-core = { path = "../core" }
ornis-macros = { path = "../macros" }
# Phase A (audit §7): shared engine for levels/edges/OrderError.
ornis-schedule = { path = "../schedule" }
serde = { version = "1", features = ["derive"] }
ron = "0.12"

[dev-dependencies]
naga = "30"
pollster = "1.0"
png = "0.18"
proptest = "1"
wgpu = "30"
glam = "0.33"
criterion = { version = "0.8", features = ["rayon"] }

[[bench]]
name = "layout_bench"
harness = false

[[bench]]
name = "recording_bench"
harness = false

[lints]
workspace = true
