# cargo-audit configuration
# Rationale for ignores — see deny.toml [advisories].
# quick-xml 0.39.4 ← wayland-scanner (build-time proc-macro, Linux only,
# trusted Wayland protocol XML); 0.41 unavailable until winit 0.30 upgrade.
[advisories]
ignore = [
    "RUSTSEC-2026-0194",
    "RUSTSEC-2026-0195",
]
