target/
.DS_Store
*.pdf
*.txt

# Debug artifacts (layout dumps, mockups) — not part of the source tree
browser_layout*.json
ui_layout*.json
bevy_editor_mockup/

# Local third-party forks referenced via path deps (kept out of git, 2+ GB)
/forks/

# Scratch / probe artifacts in repo root
/chromium_layout.json
/diff_report.json
/editor_test.html
.hermes-tmp.*
editor_test.html
*.log

# cargo-mutants output (regenerated on every run)
/mutants.out/
/mutants.out.old/
